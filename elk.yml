---
  - name: Install elastic
    hosts: elk
    become_user: root
    become: True
    roles:
      - elastic

  - name: Install filebeat
    hosts: web
    become_user: root
    become: True
    roles:
      - filebeat
...