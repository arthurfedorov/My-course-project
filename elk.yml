---
  - name: Install elastic
    hosts: elk
    become_user: root
    become: True
    roles:
      - elastic

  - name: Install kibana
    hosts: elk
    become_user: root
    become: True
    roles:
      - kibana
...